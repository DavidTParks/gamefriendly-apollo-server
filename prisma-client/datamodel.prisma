type User {
  id: ID! @id @unique
  email: String! @unique
  name: String!
  password: String!
  gameSessions: [GameSession!]!
}

type GameSession {
  id: ID! @id @unique
  title: String!
  active: Boolean!
  user: User
}